spring:
  application:
    name: account-service
  cloud:
    config:
      uri: http://localhost:8888
  profiles:
    active: dev
management:
  endpoints:
    web:
      exposure:
        include: "*"
server:
  port: 7000